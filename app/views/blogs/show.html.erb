    <header class="intro-header" style="background-image: url('/assets/post-bg.jpg')">
        <div class="container">
            <div class="row">
                <div class="col-lg-8 col-lg-offset-2 col-md-10 col-md-offset-1">
                    <div class="post-heading">
                        <h1><%= @blog.title %></h1>
                        <h2 class="subheading"><%= @blog.summary %></h2>
                        <span class="meta">Posted by <%=@blog.user.name%> on <%= @blog.created_at.strftime("%b %d, %Y") %></span>
                    </div>
                </div>
            </div>
        </div>
    </header>

    <article>
        <div class="container">
            <div class="row">
                <div class="col-lg-8 col-lg-offset-2 col-md-10 col-md-offset-1">
                <%= @blog.content %>
                <br>
                <br>
                <br>
                <%= button_to "Edit", edit_blog_path(@blog), :method => "get", class:"btn btn-default", id:"custom-btn"%>
                <%= button_to "Back", blogs_path, :method => "get", class:"btn btn-default", id:"custom-btn" %>
                </div>
            </div>
        </div>
    </article>
